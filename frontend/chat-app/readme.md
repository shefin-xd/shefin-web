# Shefin Chat frontend

Deploy at `chat.shefin.dev`. The demo uses the shared SSO client to redirect unauthenticated users to `auth.shefin.dev` and accesses chat data through the canonical `/api/chat/*` namespace.
