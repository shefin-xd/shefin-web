# Shefin Admin frontend

Deploy at `admin.shefin.dev`. The demo redirects unauthenticated visitors to the central account app and only enables its dashboard action when the central session reports `isAdmin`.
